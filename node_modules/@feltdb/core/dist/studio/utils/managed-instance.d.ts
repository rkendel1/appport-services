/**
 * FeltDB Managed Instance Utilities
 * Token generation, validation, provisioning helpers
 */
export interface ApiTokenConfig {
    id: string;
    namespace: string;
    scopes: string[];
    expiresIn?: number;
}
export interface GeneratedToken {
    id: string;
    secret: string;
    namespace: string;
    scopes: string[];
    createdAt: string;
    expiresAt: string;
}
export declare class ManagedInstanceUtil {
    private static readonly PREFIX;
    private static readonly SECRET_LENGTH;
    private static readonly CHARSET;
    static generateSecret(): string;
    static createToken(config: ApiTokenConfig): GeneratedToken;
    static validateToken(token: string): {
        valid: boolean;
        errors: string[];
    };
    static generateEnvContent(config: {
        url: string;
        token: string;
    }): string;
    static generateApiKeysJson(tokens: GeneratedToken[]): string;
}
export default ManagedInstanceUtil;
//# sourceMappingURL=managed-instance.d.ts.map