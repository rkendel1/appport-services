//#region src/utils/service-api.ts
function e(e, t) {
	return e || t?.application.application_id || "";
}
var t = class extends Error {
	constructor(e, t, n) {
		super(t), this.code = e, this.status = n;
	}
}, n = class {
	constructor(e) {
		this.options = e, this.fetcher = e.fetch ?? globalThis.fetch.bind(globalThis);
	}
	async get(e, n = {}) {
		let r = new URL(e, `${this.options.baseUrl || window.location.origin}/`);
		this.options.applicationId && r.searchParams.set("application_id", this.options.applicationId), r.searchParams.set("environment", this.options.environment);
		for (let [e, t] of Object.entries(n)) t !== void 0 && t !== "" && r.searchParams.set(e, t);
		let i = await this.fetcher(r, {
			cache: "no-store",
			headers: this.options.token ? { authorization: `Bearer ${this.options.token}` } : {}
		}), a = await i.json().catch(() => ({}));
		if (!i.ok) throw new t(a.code || "request_failed", a.message || "The Service API request failed.", i.status);
		return a;
	}
	application() {
		return this.get("/v1/application");
	}
	schema() {
		return this.get("/v1/application/schema");
	}
	records(e, t, n = {}, r = 50) {
		return this.get(`/v1/data/${encodeURIComponent(e)}`, {
			limit: String(r),
			cursor: t,
			...n
		});
	}
	record(e, t) {
		return this.get(`/v1/data/${encodeURIComponent(e)}/${encodeURIComponent(t)}`);
	}
};
function r(e) {
	return /secret|password|token|api[_-]?key|credential|private[_-]?key/i.test(e);
}
function i(e, t) {
	return r(e.name) ? t == null ? t : "[REDACTED]" : t && typeof t == "object" && !Array.isArray(t) && t.secret === !0 ? {
		configured: !!t.configured,
		secret: !0
	} : t;
}
function a(e, n) {
	return e instanceof t ? e.code === "record_not_found" ? "Record not found." : e.code === "unauthorized" ? "Sign in to inspect this application." : e.code === "forbidden" || e.code === "inspection_not_allowed" ? n ? `You don't have access to ${n}.` : "You don't have access to this application." : e.code === "invalid_cursor" ? "Invalid cursor. Refreshing from the first page." : e.message : n ? `Unable to load ${n}.` : "Unable to load application data.";
}
//#endregion
export { t as ServiceApiError, n as StudioServiceApi, r as isSecretField, e as resolveStudioApplicationId, i as safeDisplayValue, a as serviceApiErrorMessage };
