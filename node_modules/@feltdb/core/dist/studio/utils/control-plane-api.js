//#region src/utils/control-plane-api.ts
var e = class {
	constructor(e) {
		this.declaration = e, this.byId = new Map((e?.capabilities || []).map((e) => [e.id, e]));
	}
	get loaded() {
		return this.declaration !== null;
	}
	get(e) {
		return this.byId.get(e);
	}
	status(e) {
		return this.byId.get(e)?.status ?? "unsupported";
	}
	usable(e) {
		return this.status(e) !== "unsupported";
	}
	note(e) {
		return this.byId.get(e)?.note || "This FeltDB instance does not declare this capability.";
	}
	area(e) {
		return (this.declaration?.capabilities || []).filter((t) => t.area === e);
	}
}, t = class extends Error {
	constructor(e, t, n) {
		super(t), this.code = e, this.status = n;
	}
}, n = class {
	constructor(e = {}) {
		this.options = e, this.fetcher = e.fetch ?? globalThis.fetch.bind(globalThis);
	}
	origin() {
		return this.options.baseUrl ? this.options.baseUrl.replace(/\/$/, "") : typeof window > "u" ? "" : window.location.origin;
	}
	url(e, t = {}, n = !1) {
		let r = new URL(e, `${this.origin()}/`);
		n && (this.options.applicationId && r.searchParams.set("application_id", this.options.applicationId), r.searchParams.set("environment", this.options.environment || "production"));
		for (let [e, n] of Object.entries(t)) n !== void 0 && n !== "" && r.searchParams.set(e, n);
		return r.toString();
	}
	async request(e, n = {}) {
		let r = await this.fetcher(e, {
			...n,
			cache: "no-store",
			headers: {
				...n.body ? { "content-type": "application/json" } : {},
				...this.options.token ? { authorization: `Bearer ${this.options.token}` } : {},
				...n.headers || {}
			}
		}), i = await r.json().catch(() => ({}));
		if (!r.ok) throw new t(i.code || "request_failed", i.message || i.error || `The FeltDB control plane refused the request (${r.status}).`, r.status);
		return i;
	}
	get(e, t = {}, n = !1) {
		return this.request(this.url(e, t, n));
	}
	post(e, t) {
		return this.request(this.url(e), {
			method: "POST",
			body: JSON.stringify(t)
		});
	}
	async capabilities() {
		return new e(await this.get("/v1/control-plane/capabilities"));
	}
	overview() {
		return this.get("/v1/control-plane/overview");
	}
	diagnostics() {
		return this.get("/v1/diagnostics");
	}
	developer() {
		return this.get("/v1/control-plane/developer", {}, !0);
	}
	actions() {
		return this.get("/v1/control-plane/actions", {}, !0);
	}
	indexes() {
		return this.get("/v1/control-plane/indexes", {}, !0);
	}
	rebuildIndex(e, t) {
		return this.post("/v1/control-plane/indexes/rebuild", {
			application_id: this.options.applicationId,
			environment: this.options.environment || "production",
			collection: e,
			field: t
		});
	}
	runAction(e, t, n) {
		let r = encodeURIComponent(this.options.applicationId || ""), i = encodeURIComponent(e), a = this.url(`/api/applications/${r}/revisions/${i}/actions/${encodeURIComponent(t)}`, { environment: this.options.environment || "production" });
		return this.request(a, {
			method: "POST",
			body: JSON.stringify(n)
		});
	}
	operations(e = {}) {
		return this.get("/v1/control-plane/operations", {
			collection: e.collection,
			kind: e.kind,
			limit: e.limit === void 0 ? void 0 : String(e.limit)
		});
	}
	transactions(e = {}) {
		return this.get("/v1/control-plane/transactions", {
			subject: e.subject,
			collection: e.collection,
			limit: e.limit === void 0 ? void 0 : String(e.limit)
		});
	}
	transaction(e) {
		return this.get(`/v1/control-plane/transactions/${encodeURIComponent(e)}`);
	}
	provenance(e, t, n = !0) {
		return this.get(`/v1/control-plane/provenance/${encodeURIComponent(e)}/${encodeURIComponent(t)}`, {}, n);
	}
	agents() {
		return this.get("/v1/control-plane/agents", {}, !0);
	}
	agent(e) {
		return this.get(`/v1/control-plane/agents/${encodeURIComponent(e)}`, {}, !0);
	}
	registerAgent(e) {
		let t = encodeURIComponent(this.options.applicationId || "");
		return this.request(this.url(`/api/applications/${t}/principals`), {
			method: "POST",
			body: JSON.stringify({
				kind: "agent",
				name: e
			})
		});
	}
	grantAgentCapability(e, t) {
		return this.request(this.url(`/api/principals/${encodeURIComponent(e)}/capabilities`), {
			method: "POST",
			body: JSON.stringify({ capability: t })
		});
	}
	startAgentSession(e) {
		return this.request(this.url(`/api/agents/${encodeURIComponent(e)}/workloads`), {
			method: "POST",
			body: "{}"
		});
	}
	workloads() {
		return this.get("/v1/workloads", { application_id: this.options.applicationId });
	}
	replication() {
		return this.get("/v1/control-plane/replication");
	}
	applications() {
		return this.get("/v1/control-plane/applications");
	}
	stateResources(e = {}) {
		return this.get("/v1/state/resources", {
			prefix: e.prefix,
			limit: e.limit === void 0 ? void 0 : String(e.limit)
		});
	}
	stateHistory(e) {
		return this.get(`/v1/state/resources/${encodeURIComponent(e)}/history`);
	}
	stateRevision(e) {
		return this.get(`/v1/state/revisions/${encodeURIComponent(e)}`);
	}
	stateDiff(e, t) {
		return this.post("/v1/state/diff", {
			from: e,
			to: t
		});
	}
	stateConflicts(e, t, n) {
		return this.post("/v1/state/conflicts", {
			base: e,
			left: t,
			right: n
		});
	}
	reconciliationPlan(e) {
		return this.post("/v1/state/reconciliation/plan", e);
	}
	reconciliationExecute(e) {
		return this.post("/v1/state/reconciliation/execute", e);
	}
};
function r(e) {
	return e.length ? e.map((e) => typeof e == "number" ? `[${e}]` : e).join(".").replace(/\.\[/g, "[") : "(root)";
}
function i(e, n) {
	if (!(e instanceof t)) return n ? `Unable to load ${n}.` : "Unable to reach the FeltDB control plane.";
	switch (e.code) {
		case "revision_not_found": return "That revision is not held by this instance.";
		case "invalid_plan": return `The reconciliation plan is not usable: ${e.message}`;
		case "plan_refused": return `FeltDB refused the plan: ${e.message}`;
		case "index_not_declared": return "The application contract declares no index on that field.";
		case "application_required": return "No application connected. Connect Studio to a FeltDB application to inspect its data.";
		case "unauthorized":
		case "AUTHENTICATION_REQUIRED": return "Sign in to operate this instance.";
		case "forbidden":
		case "FORBIDDEN": return n ? `You do not have access to ${n}.` : "You do not have access to this operation.";
		default: return e.message;
	}
}
//#endregion
export { e as CapabilitySet, n as ControlPlaneApi, t as ControlPlaneError, i as controlPlaneErrorMessage, r as formatStatePath };
