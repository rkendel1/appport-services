export type ContractGapType = 'MISSING' | 'AMBIGUOUS' | 'CONTRADICTORY' | 'UNAUTHORIZED' | 'UNSUPPORTED' | 'UNVALIDATED' | 'DEPENDENCY_MISSING' | 'PROVIDER_MISSING';
export type ContractGap = {
    contract_path: string;
    gap_type: ContractGapType;
    severity: string;
    blocking: boolean;
    confidence: number;
    suggested_questions: string[];
    affected_paths: string[];
};
export type ContractAssertion = {
    value: unknown;
    confidence: number;
    evidence_refs: string[];
    required: boolean;
    validated: boolean;
};
export type ApplicationContract = {
    contract_id: string;
    application_id: string;
    base_revision?: string;
    contract_revision: number;
    status: string;
    assertions: Record<string, ContractAssertion>;
    gaps: ContractGap[];
};
export type ContractReadiness = {
    status: string;
    readiness: number;
    blocking_gaps: number;
    gaps: ContractGap[];
    confidence: Record<string, number>;
    next_action: string;
};
export declare class ApplicationContractClient {
    private request;
    constructor(request: (path: string, init?: RequestInit) => Promise<any>);
    get(id: string): Promise<ApplicationContract>;
    evaluate(id: string, outcome: string): Promise<ApplicationContract>;
    readiness(id: string): Promise<ContractReadiness>;
    gaps(id: string): Promise<{
        gaps: ContractGap[];
    }>;
    question(id: string): Promise<any>;
    end(id: string): Promise<ApplicationContract>;
    construct(id: string): Promise<any>;
    patch(id: string, patch: unknown): Promise<ApplicationContract>;
}
//# sourceMappingURL=application-contract.d.ts.map