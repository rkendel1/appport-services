/**
 * Analytics Backend - Dynamic Rust/WASM with TypeScript fallback
 *
 * Provides a unified interface to analytics with automatic fallback:
 * - Primary: Rust-based implementation via WASM for performance
 * - Fallback: Pure TypeScript implementation when WASM unavailable
 */
export interface IAnalyticsBackend {
    recordIndexUsage(index_name: string, operation: 'read' | 'write', time_ms: number): void;
    recordQuery(fields: string[], operator: string, executionTimeMs: number, rowsScanned: number, resultCount: number, indexUsed?: string): void;
    getIndexMetrics(index_name: string): any | null;
    getAllMetrics(): any[];
    getSlowestQueries(limit: number): any[];
    getFrequentFields(limit: number): Array<[string, number]>;
    generateReport(collectionName: string, period: string): any;
    reset(): void;
}
/**
 * Analytics backend selector with dynamic WASM loading
 */
export declare class AnalyticsBackend implements IAnalyticsBackend {
    private backend;
    private wasmLoaded;
    constructor();
    private loadWasm;
    recordIndexUsage(index_name: string, operation: 'read' | 'write', time_ms: number): void;
    recordQuery(fields: string[], operator: string, executionTimeMs: number, rowsScanned: number, resultCount: number, indexUsed?: string): void;
    getIndexMetrics(index_name: string): any | null;
    getAllMetrics(): any[];
    getSlowestQueries(limit: number): any[];
    getFrequentFields(limit: number): Array<[string, number]>;
    generateReport(collectionName: string, period: string): any;
    reset(): void;
}
/**
 * Create analytics instance - internal use only
 */
export declare function createAnalyticsBackend(): IAnalyticsBackend;
//# sourceMappingURL=analytics-backend.d.ts.map