# API Keys Capability

This repository currently implements one AppPort Services capability: tenant-scoped API keys.

See `src/appport_services/api_keys/` for the semantic implementation and `docs/api-keys.md` for usage and lifecycle details.
