# create-appport

Create a contract-driven AppPort application:

```sh
npx create-appport my-app
cd my-app
npm run dev
```

The generated application contains business behavior only. `appport.toml` declares the infrastructure materialized by `@appport/runtime`.
