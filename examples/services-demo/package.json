{
  "name": "services-demo",
  "version": "0.1.0",
  "description": "AppPort Services example application demonstrating API Keys, Webhooks, and Jobs",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/src/app.js",
    "test": "npm run build && node --test dist/tests/**/*.test.js"
  },
  "dependencies": {
    "@appport/runtime": "file:../../packages/runtime"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.6.3"
  }
}
